Program to read/write time for the IRIG-B project.
This is intended to work on a MicroZed 7020 using 2 of the GPIO.

To compile file run
```
sudo make
```
To run do
```
sudo ./irigb_ctrl
```
and choose from the 3 options: `Read` `Write` or `Quit`
